/* SN ProDG libm: the fdlibm source is compiled as C++ through this wrapper (static consts
 * defined in an included file are dropped when folded, tables keep their small-data placement). */
#include "fdlibm/e_rem_pio2.c"
