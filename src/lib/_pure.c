/* libgcc: gcc 2.95.3 libgcc2.c section L_pure.
 * __pure_virtual: the handler a pure virtual call lands in (prints "pure virtual method called" and
 * aborts); every C++ class with pure virtuals in the game has its vtable slots pointing here. */
#define L_pure
#include "libgcc2/libgcc2.c"
