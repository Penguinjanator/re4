// game/light04: light type 4, the foot shadow light (D:/Bio4/Prog/light04.cpp). It has no per-frame
// behaviour; foot_shadow.cpp reads its work (mode 5, direction, height) to cast character shadows.
#include "light.h"

// LightFuncTbl[4]: nothing to do.
void Light04_Move(cLight* pLi)
{
}
